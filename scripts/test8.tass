//this file is to test loops
gvar rectX = 10
gvar rectDx
gvar count
do {
    fill(3500)
    stroke(3500)
    clear()
    fill(256)
    stroke(256)
    rectDx = rectX
    rectDx += 10
    count = 0
    //note local vars declared outside the loop are not viable inside the loop
    do {//note to self perhaps add a way to declair and initialize local vars just outside the loop
        lvar rtop = count
        rtop *= 12

        drect(rectX,rectDx,rtop)
        count ++
    } while( count < 5)

    rectX ++
    rectX %= 53
    update()
} while (0 == 0)

function drect(left, right, top){
    lvar bottom  = top
    bottom += 10
    rect(left, top,right,bottom)
}